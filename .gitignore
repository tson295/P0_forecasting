# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Smoke output tạm (data tổng hợp, không phải kết quả)
tmp_smoke*/

# Raw data: CSV lớn không vào git (integrity anchor: data/data_checksums.json)
data/*.csv

# Artifacts sinh ra khi chạy experiment (log.csv canonical thì được track)
experiments/runs/
experiments/cache/
reports/figures/

# Claude Code local-only
.claude/settings.local.json

# Log bootstrap thô (build LightGBM, pip) — không cần track
experiments/bootstrap.log
