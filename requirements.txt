# P0_forecasting — môi trường Vast (Linux, CUDA). Pin phiên bản thực tế được ghi vào experiments/env.txt khi bootstrap.
numpy>=2.0
pandas>=3.0
scipy>=1.14
scikit-learn>=1.6
matplotlib>=3.9
pytest>=8
xgboost>=3.0          # device=cuda
catboost>=1.2         # task_type=GPU
# lightgbm: cài BUILD GPU (OpenCL) trong scripts/vast_bootstrap.sh — wheel pip mặc định không có GPU:
#   pip install lightgbm --no-binary lightgbm --config-settings=cmake.define.USE_GPU=ON
# torch: cài theo CUDA của image (vd. pip install torch --index-url https://download.pytorch.org/whl/cu128)
# TimesFM (§2.2 #4) và AutoTS (§2.2 #6) — adapter đã code; audit xong (docs/reference/audit_timesfm.md, audit_autots.md).
# CÀI KHI USER CHO PHÉP (plan §2.2: "cài package chỉ khi user cho phép"):
#   pip install "timesfm[torch]==2.0.2"          # checkpoint google/timesfm-2.5-200m-pytorch, rev 1d952420…, 925 MB, public
#   pip install autots==1.0.4 statsmodels        # statsmodels là dep bắt buộc của autots
#   # chỉ khi chạy TimesFM covariate: pip install "jax[cpu]" scikit-learn   (jax[cuda] xung đột wheel với torch cu128)
#   # chỉ khi chạy TFM-LoRA: pip install "transformers>=5.4" peft accelerate  (checkpoint …-2.5-200m-transformers)
