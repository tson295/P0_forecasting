| Paper | Câu hỏi | Ý tưởng / Cách làm | Kết quả chính | Takeaway |
|---|---|---|---|---|
| **1. Deep Order Flow Imbalance** | Representation (raw LOB vs OF) hay architecture quan trọng hơn? | OF = mức tăng/giảm volume bid/ask giữa t−1 và t; so sánh 2 loại input trên cùng architecture | OF > raw LOB; plain LSTM + OF rất mạnh; thêm CNN hoặc LSTM sâu hơn không đáng | **Representation > architecture**; horizon hiệu dụng ≈ 2 lần mid-price đổi giá |
| **2. Improving Deep Learning of Alpha Term Structures** | Architecture chuyên multi-horizon có hơn LSTM? Design choice nào quan trọng? | Controlled test 4 model; thử input, depth/width, window, elapsed time | Không model nào vượt plain LSTM ổn định; window dài ≠ tốt (info LOB decay nhanh); thêm elapsed time giúp ích | **Data/input > architecture**; event index thiếu thông tin thời gian (lỗ hổng của paper 1) |
| **3. HFformer** | Transformer có lợi thế cho HFT không? | Transformer encoder + linear decoder, spiking activation, bỏ positional encoding; input gần raw LOB | Linear decoder là cải thiện lớn nhất (spiking, PE không đáng kể); data nhỏ thì LSTM thắng, tăng data thì HFformer cải thiện tương đối; backtest 3-signal: 120 vs 9.7 USDT | Transformer **cần nhiều data**; PnL chỉ tham khảo (2 ngày, nhiều giản lược) |
| **4. Ben Taieb et al. (2010)** *[bổ sung]* | Multi-step: 1 model output cả vector H bước có tốt hơn single-output (Recursive/Direct)? | MIMO: `X_t → [y_{t+1}, …, y_{t+H}]`, giữ dependency giữa các horizon; Recursive thì cộng dồn lỗi, Direct thì bỏ qua dependency | Multiple-output thường tốt hơn Recursive và Direct | Plain LSTM output vector `[r_h1 … r_hK]` ở paper 1–2 chính là dạng MIMO |
| **5. Chevillon (2007)** | Train 1-step rồi iterate h lần, hay train thẳng cho horizon h (DMS)? | Mỗi horizon 1 bộ tham số: `θ_h = argmin Σ (y_{t+h} − f(X_t; θ))²`; survey lý thuyết + empirical | Không có kết luận "Direct luôn hơn"; Direct có lợi khi non-stationary, structural/location shift, misspecification | Lợi thế Direct = **robust khi model sai**, không phải vì recursive luôn tệ |
| **6. Briola et al. (2024)** | Tín hiệu DeepLOB có đủ mạnh để tạo lợi nhuận thực? | *[bổ sung]* Framework open-source LOBFrame, dự báo mid-price change trên nhiều cổ phiếu NASDAQ; thêm metric là xác suất dự đoán đúng trọn một transaction | *[bổ sung]* Microstructure của cổ phiếu quyết định mức độ predictable; forecast tốt ≠ tín hiệu trade được | Metric ML truyền thống không đủ, phải đánh giá theo góc độ giao dịch | 


