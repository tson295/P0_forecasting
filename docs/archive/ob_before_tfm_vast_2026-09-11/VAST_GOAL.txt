Tiếp tục MỘT GOAL P0_forecasting trên Vast hiện có: tự giải quyết data rồi chạy đủ src_OB, metrics/latency/report và lưu công việc. Tôi đi ngủ; tự làm trong scope sau, không hỏi duyệt từng bước.

Đọc .claude/{CLAUDE,MEMORY,AGENT}.md, .claude/agents/checker.md, src_OB/README.md, config và reports thật. Nếu docs/VAST_SESSION_PROMPT.md đã cập nhật sau blocker HF thì theo đó; prompt này thay quy định HF-only cũ.

1. Tiếp nối e169df1/OB trên Vast, không reset/ghi đè. Đọc RUN_REPORT, DATA_REPORT, CHECKER_FINDINGS; HF đã BLOCKED, 0 cell, không lặp tải/prepare archive lỗi. Reuse GPU env đã build; không cài JAX/XReg.

2. Lưu việc ngay: push qua auth sẵn có. Thiếu auth thì tạo Git bundle + gói reports/logs/config/metadata/artifact và LFS objects chưa push; chuyển tới đích ngoài instance đã được cấp quyền nếu có. Bundle trên ổ Vast chưa phải backup. Thiếu đích/auth: ghi BACKUP_PENDING, tiếp tục việc khác. Không lộ secret, git add -A, force push, stop/destroy/thuê instance.

3. Đọc/sửa W1 snapshot/buffer/replay theo evidence và semantics nguồn; không nối gap, replay trùng hoặc dùng snapshot tương lai tạo state quá khứ. Giữ prepared cũ, tạo version mới nếu fix ảnh hưởng kết quả.

4. Tự tìm/chọn historical BTCUSDT Binance Spot L2 public miễn phí hoặc nguồn project đã có quyền dùng. Được đổi nguồn/revision, thêm downloader/adapter/config riêng, pin manifest/hash, ghi SOURCE_REPORT. Không tự mua data, đổi thị trường/symbol, dùng OHLCV/trades/L1/synthetic thay L2 hay nối phần thiếu. Không cần đủ 2 năm; lấy coverage/fold thật. Đọc metadata/dung lượng trước tải, không làm đầy disk. Tìm nguồn chủ động tối đa khoảng 60 phút; không ngắt download/prepare/train đang tiến triển. Không chờ upstream/thu mới nhiều tuần. Không có nguồn phù hợp: làm hết sửa lỗi/report/backup rồi báo BLOCKED, không hỏi tôi giữa đêm.

5. Giữ top10 bid+ask, một mid; diff quantity tuyệt đối, 0 xóa, gom message, kiểm tra U/u, prune đúng. Gap ID/timestamp đóng segment, chờ snapshot. Hard gap July5 thuộc HF, không áp mù lên nguồn độc lập. OF trước same-mid drop, cộng flow, OFI 10 level; raw timeline riêng. Baseline OF/OFI+timing, không distance/DeepLOB mới. Direct h60/120/180, label log(MP(t+h)/MP(t)), as-of <=t+h/age guard, context/label cùng segment. Giữ config/context/seed/budget: FIT21d/gap6d/VAL3d/step7d, tối đa5 fold; common mask đủ mọi family/horizon, TimesFM512. Không giảm cấu hình ép data đạt.

6. Download/prepare/data-report thật bằng config đã chọn. Checker chỉ đọc evidence sau prepare/khi có lỗi correctness/cuối run; session chính sửa và train. FIT/VAL hợp lệ thì FULL ngay: P0_OB_VAST=1, CUDA_VISIBLE_DEVICES=0; python -m src_OB train --config <config-thật>, thành công rồi summarize cùng config. Đủ lgbm,xgb,cat,xgbrf,lstm,autots,tfm_zero_shot,tfm_lora × mọi fold × 3h. AutoTS GPU allowlist search; zero-shot chỉ mid; LoRA+OF head không XReg. Fit GPU-only trên Vast, cấm CPU fallback/local training. Cấm test/smoke/canary/probe/trial fit/benchmark/warmup riêng. Giữ tmux/log, sửa lỗi thật rồi tiếp tục; recovery theo cell có provenance, không ghi đè completed.

7. RMSE/MAE/R² raw price; rmse_gain_vs_e0/r2_os_vs_e0 reuse cùng E0/origins. Latency inference thật p95/p99 batch1/max quan sát. COMPLETE chỉ khi >=1 fold hợp lệ, đủ folds×8×3 cell với completed/metrics/predictions/latency/checkpoint, summaries/reports/checker findings. Cập nhật MEMORY, commit/push scope OB, lưu backup status. BLOCKED phải có evidence, nguồn đã loại, cell thiếu, vị trí backup hoặc pending và next step. Không hoàn tất giả; không dừng vì compact/data xong/một model xong. Bắt đầu ngay.
