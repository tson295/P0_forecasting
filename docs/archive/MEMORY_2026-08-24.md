> **ARCHIVED 2026-08-27 — không còn hiệu lực.** Bản này được thay bằng plan 4 bước đơn giản hóa trong `docs/RESEARCH_PLAN.md` / `.claude/CLAUDE.md`. Giữ chỉ để tham khảo lịch sử.

# MEMORY — trạng thái nghiên cứu (evolving; update/replace, không append mâu thuẫn)

PHASE: RESEARCH / GOVERNANCE / P0 DATA-INTEGRITY BLOCKED
TRAINING: LOCKED

## Current Task

2026-08-24: đã hoàn tất audit read-only và cập nhật roadmap/governance theo quyết định user mới nhất. Không training, không download data, không install package, không load TimesFM checkpoint nặng. Chưa có src/ hoặc experiment run thật.

## Exact Next Step

Trong TRAINING_LOCKED:

1. thiết kế/implement static P0 validator và canonical lowercase→uppercase schema adapter ở ingestion layer, không sửa Baseline_LGBM.py;
2. tiếp tục FEATURE DISCOVERY research, lập family dossiers cho X1–X5 và D1–Dk, rồi freeze finite Wave-1;
3. hoàn tất TimesFM version/API audit và LSTM architecture review ở mức tài liệu;
4. chỉ sau explicit user authorization mới khôi phục canonical data; sau restoration phải verify row/range/UTC/grid/gap/schema, tạo data_checksums.json và freeze data version.

Không chạy real fit cho tới khi cả TRAINING được user unlock và P0 gate PASS.

## Completed (chỉ việc đã thực làm)

- 2026-08-24: audit toàn bộ code/docs/config/manifests/data shape; ba research nhánh yfinance, AutoTS, arXiv:2407.18334v1 + LSTM hoàn tất; không sửa file trong audit.
- 2026-08-24: sửa RESEARCH_PLAN, CLAUDE, MEMORY và affected governance references theo point-only objective, scope mới, P0 gate, feature discovery, Near/Far và TimesFM/LSTM protocol.
- 2026-08-22: tạo .claude system, roadmap ban đầu và git metadata.
- 2026-08-21: chốt B0 frozen, Gain metric, initial 60-day dev budget và VWAP chỉ dạng proxy có tên rõ.

## Research Decisions (canonical, mới nhất trước)

- 2026-08-24 [FROZEN]: Primary objective duy nhất = POINT FORECASTING trên y_h(t)=log(C[t+h]/C[t]), h=1,2,3. Mọi tree/TimesFM/LSTM survivor sinh ba scalar point predictions.
- 2026-08-24 [FROZEN]: Scope = frozen LightGBM B0 + LightGBM candidate/XGBoost/CatBoost/eligible tree ensemble; TimesFM native/custom point/LoRA; đúng một LSTM research exception, proposal LSTM-DMH-512. Không mở DL/model family khác.
- 2026-08-24 [FROZEN]: B0 vẫn frozen; schema/objective/ES/calibration variant phải nằm ngoài Baseline_LGBM.py và có tên explicit.
- 2026-08-24 [FROZEN]: Track B distribution objective và CRPS/pinball/coverage/sharpness contract bị obsolete. Quantile/path của TimesFM chỉ là internal point-estimation mechanism.
- 2026-08-24 [FROZEN]: Metric contract không tự mở rộng: Gain, MedianGain, WinRate, P10/Worst; secondary RMSE, MAE, Pearson r, directional accuracy và existing importance/diagnostics. Feature/hypothesis được auto-discover; metric không.
- 2026-08-24 [FROZEN]: X1–X5 là seed, không phải whitelist. Research phải thiết kế finite D1–Dk Wave-1 với hypothesis/formula/lookback/causality/redundancy/data/compute/leakage dossier trước real VAL.
- 2026-08-24 [FROZEN]: Final holdout = HOLDOUT-NEAR + HOLDOUT-FAR, cả hai one-shot trong cùng final stage; không sửa model sau Near trước Far. Partition eligibility dựa toàn bộ target availability, không chỉ origin timestamp.
- 2026-08-24 [FROZEN]: yfinance = optional auxiliary data audit, không thay canonical exchange data; AutoTS = optional constrained harness, không phải candidate; paper 2407.18334v1 = reference only, không mở scope.
- 2026-08-24 [FROZEN]: TRAINING_LOCKED tiếp tục; P0 data-integrity gate phải PASS trước bất kỳ real experiment nào.

## Experiment Findings

(chưa có — chưa chạy experiment thật; flat forecast hiện là hypothesis, không phải finding)

## Data / Implementation Blockers

- Local OHLCV snapshot không khớp manifest: BTC/ETH/SOL/XRP HF actual data rows lần lượt khoảng 21.917/22.999/24.579/21.618, trong khi expected khoảng 289k; LF cũng thiếu lớn. Nhiều file dừng ở fixed byte size và thiếu terminal newline.
- Không được coi local truncated snapshot là canonical hoặc tạo canonical checksum từ nó.
- data/data_checksums.json hiện chưa tồn tại dù governance cũ giả định có.
- Raw CSV header lowercase open/high/low/close/volume; B0 require Open/High/Low/Close/Volume. Cần ingestion/schema adapter, không sửa B0.
- Exact restored snapshot/range chưa biết, nên Near/Far timestamps và Far separation còn [PLAN].

## Bugs / Pitfalls

- Baseline_LGBM.py: objective Huber, alpha 0,90; TargetTransform fit train-only; early stopping thực tế monitor Huber. RMSE là candidate-selection metric. Alignment research phải là explicit variant.
- Baseline config require_p100=True/device_type=gpu viết cho Kaggle; real run sau unlock dùng generic GPU detection trên Vast, không CPU fallback.
- pandas 3.0.3: CoW default; dựng UTC index từ unix timestamp; dùng min thay alias T.
- Local chưa cài lightgbm/xgboost/catboost/timesfm/AutoTS/yfinance; không cài trong current task.
- HYPE canonical OHLCV dự kiến quá ngắn cho initial xasset history; không tự bù bằng close-only/Yahoo.
- Target h=2,3 chồng lấp; existing daily-block analysis giữ nguyên.
- TimesFM behavior phụ thuộc exact package/checkpoint/backend/API; infer_is_positive hoặc equivalent cho signed returns phải audit. Flatline GitHub issue không phải proof model lỗi.
- Hook/frontmatter notes cũ vẫn chưa được runtime-verify; không liên quan research scope.

## Rejected / Superseded Approaches

- Track B distribution forecasting và canonical CRPS/pinball/coverage/sharpness/tail metrics — superseded bởi point-only decision.
- Holdout 30 ngày bắt đầu ngay sau Dev — superseded bởi Near/Far với embargo và target-boundary contract.
- TimesFM-only DL invariant — superseded đúng phạm vi bởi LSTM-DMH research exception.
- GRU, TCN, MLP sequence, generic Transformer, PatchTST, Chronos, TimeGPT và DL family khác — vẫn out of scope.
- Random Forest/Extra Trees/SGD hoặc paper/AutoTS model zoo — không thuộc current model scope.
- AutoTS default model/metric/split/search; yfinance làm canonical replacement hoặc missing-row filler.
- CPU full training, silent CPU fallback, fetch data khi user chưa cho phép.

## Important Files

- Baseline_LGBM.py — B0 frozen, deny-protected.
- docs/RESEARCH_PLAN.md — canonical roadmap chi tiết.
- .claude/CLAUDE.md — stable invariants.
- .claude/MEMORY.md — current state và decision log hiện có.
- data/manifest.json — intended OHLCV contract; không chứng minh local bytes đầy đủ.
- data/data_checksums.json — phải tạo sau restoration; hiện chưa tồn tại.
- timesfm_ohlcv_distribution_forecasting_R0_R6.md — legacy distribution research reference; mapping point-oriented canonical nằm trong RESEARCH_PLAN.
- g_research_crypto_solutions_summary.md — literature/reference only; không có quyền đổi model/metric scope.

## Open Questions [PLAN]

- Exact canonical restored snapshot, row counts, timestamps và data version.
- Exact HOLDOUT-NEAR/HOLDOUT-FAR timestamps; Far temporal gap; final refit/inference schedule.
- Exact D1–Dk families, Wave-1 ordering và có cho phép exploratory second wave hay không.
- Exact TimesFM package/checkpoint/revision/backend; native point/quantile API; context; signed-return behavior; LoRA support; point-oriented custom candidates.
- Final LSTM-DMH architecture details, context/hidden/layers/training config; base vs B* follow-up.
- Exact ordering TimesFM vs LSTM sau tree attribution.
- Optional AutoTS/yfinance use — không active nếu chưa có explicit user approval.
- Vast instance/environment decisions chỉ khi chuẩn bị unlock.
