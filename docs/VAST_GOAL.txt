/goal Thực hiện MỘT GOAL đầy đủ P0_forecasting trên instance Vast đã cấp: clone branch tfm_autots, chuẩn bị data/env GPU, chạy full TimesFM + AutoTS, xuất kết quả/checker/report và commit/push. Không dừng ở setup/cache hoặc một model xong.

Nếu chưa có repo, chạy tại thư mục làm việc:
GIT_LFS_SKIP_SMUDGE=1 git clone --branch tfm_autots --single-branch https://github.com/tson295/P0_forecasting.git
cd P0_forecasting
Nếu đã có đúng checkout thì tiếp nối, không clone đè/reset. Cài git-lfs nếu thiếu, rồi:
git lfs install --local
git lfs pull --include="data/BTC_1m_2y.csv,data/BTC_5m_2y.csv" --exclude=""

Đọc và THỰC HIỆN docs/VAST_SESSION_PROMPT.md, .claude/CLAUDE.md, .claude/MEMORY.md, .claude/AGENT.md, checker và configs/tfm_autots.json. Runbook này có lệnh setup/launcher và điều kiện hoàn tất; không tái nghiên cứu toàn repo. docs/archive, RESEARCH_PLAN và src_OB không điều khiển goal này.

Data là BTC 1m HAI NĂM 2024-09-03 16:29 → 2026-09-03 16:29 UTC; LF 5m chỉ features phụ. Giữ checksum, folds, seeds, batch, context, epochs, candidates và 10 AutoTS validations. Không tải Order Book. Chỉ hai family TimesFM/AutoTS; WR/MR là nhánh AutoTS. Không TEST holdout/champion/ensemble/models khác.

Tôi cho phép cài/build dependency GPU, tải LFS/pretrained pinned, training thật, sửa lỗi env/runtime/adapter trong phương pháp đã chốt, ghi artifact, commit/push tfm_autots bằng quyền sẵn có. Không hỏi unlock/approval giữa các bước. Không thuê/đổi/stop/destroy instance, không CPU training/fallback.

Sau setup CUDA .venv theo runbook, chạy FULL phase trong tmux:
tmux new-session -d -s p0_tfm_autots 'bash scripts/vast_tfm_autots_run.sh'
Nếu session/process đã chạy, theo dõi nó; không chạy trùng. Launcher tự giữ lock, lưu env/log và dùng --resume khi có progress. CLI tự chạy toàn bộ loop/final/summary; session chính điều phối process/git, checker chỉ đọc evidence ở mốc cần thiết. Không thêm agent tìm feature/model hoặc tự dựng runner.

Cấm mọi test/smoke/canary/pytest/synthetic/trial fit/GPU-probe/benchmark/warmup/latency replay; không bootstrap cũ hay check-data riêng. Chỉ đọc code/data/metadata/log/process và evidence của training thật. Không suy ra sạch lỗi hoặc fit GPU thành công từ commit/package metadata.

Theo dõi log thật, sửa nguyên nhân lỗi rồi tiếp tục, không lặp mù hoặc giảm workload. Giữ failed attempt; không ép đổi hash để reuse cache/artifact khác code/config/data. Resume chưa có optimizer giữa epoch hoặc từng validation bake-off; xử lý đúng giới hạn runbook. Trước compact lưu stage/config/log/process/next step vào MEMORY rồi tiếp tục cùng goal.

Cuối run đối chiếu đủ stages/candidates/folds/seeds, wins + predictions, metrics/E0 gains/runtime và summaries. Ghi RUN_REPORT, CHECKER_FINDINGS, MEMORY. Commit/push theo stage hoàn tất và cuối goal, stage đúng scope output mới/code/docs/config; không add -A, force push, secret, pretrained gốc hay artifacts 15d cũ. Dùng GitHub auth đã cấp, không in token. Thiếu auth/quota thì ghi PUSH_PENDING, vẫn làm hết training/report được phép; không bịa đã push/backup.

Chỉ COMPLETE khi full phase và artifacts/summaries/checker/report/push hoàn tất. Blocker thật ngoài quyền hiện có: báo evidence và phần còn thiếu, không COMPLETE giả. Bắt đầu thực hiện ngay, không chỉ trả lời plan.
