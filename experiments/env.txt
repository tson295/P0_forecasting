== GPU ==
0, NVIDIA RTX 5000 Ada Generation, 595.84, 32760 MiB
1, NVIDIA RTX 5000 Ada Generation, 595.84, 32760 MiB
GPU khả dụng: 2 (0,1) → export P0_GPU_DEVICES=0,1 (mặc định 1 task nặng/GPU); kiểm định tuyến: python run.py gpu-probe --config configs/p0_ml_lstm.json
== apt: OpenCL + boost (cho LightGBM build GPU) ==
apt: bỏ qua (uid=1000, không phải root) — dùng toolchain có sẵn
thiếu Boost → build LightGBM OpenCL sẽ fail, fallback USE_CUDA
Platform #0: NVIDIA CUDA
 +-- Device #0: NVIDIA RTX 5000 Ada Generation
 `-- Device #1: NVIDIA RTX 5000 Ada Generation
Platform #1: Portable Computing Language
 `-- Device #0: cpu-haswell-AMD EPYC 7J13 64-Core Processor
== pip ==
== package cho TimesFM + AutoTS (§2.2 #4/#6) ==
jax devices: [CudaDevice(id=0), CudaDevice(id=1)]
== LightGBM: build GPU rồi RESOLVE backend thật sự fit được ==
build LightGBM OpenCL thất bại → thử build CUDA
lightgbm 4.7.0
LightGBM device_type=gpu FAIL: LightGBMError: GPU Tree Learner was not enabled in this build.
Please recompile with CMake option -DUSE_GPU=1
LightGBM fit OK với device_type=cuda
→ đã ghi models.lgbm.device_type=cuda vào configs/p0_ml_lstm.json (AutoTS-WR + template bake-off dùng chung backend này)
== preflight GPU: torch / XGBoost / CatBoost / LightGBM(B0 assert) ==
numpy 2.5.2
pandas 3.0.5
scipy 1.18.1
sklearn 1.9.0
lightgbm 4.7.0
xgboost 3.4.1
catboost 1.2.10
torch 2.11.0+cu128
matplotlib 3.11.1
timesfm 2.0.2
autots 1.0.4
statsmodels 0.15.0
jax 0.11.1
torch.cuda: True NVIDIA RTX 5000 Ada Generation
XGBoost device=cuda: OK
CatBoost task_type=GPU: OK
LightGBM assert_p100 (device_type=cuda): OK
== git lfs ==
Updated Git hooks.
Git LFS initialized.
== unit test (CPU, không training) ==
bootstrap OK — backend đã resolve và ghi vào configs/p0_ml_lstm.json; xem experiments/env.txt
bước tiếp: PYTHONPATH=src:. python scripts/vast_canary.py --config configs/p0_ml_lstm.json → python run.py gpu-probe --config configs/p0_ml_lstm.json (mỗi worker phải báo đúng GPU vật lý của nó)
